version: '3'
services:
  app:
    image: "111111.dkr.ecr.us-east-1.amazonaws.com/curiosityio/api:beta-$API_VERSION"
    user: node
    command: npm run _production
    restart: unless-stopped
    env_file:
      - ../../.env.staging

  redis:
    restart: unless-stopped
    volumes:
      - /etc/docker/redis-storage/app/:/usr/local/var/db/redis/

networks:
  default:
    external:
      name: nginx-proxy-network
